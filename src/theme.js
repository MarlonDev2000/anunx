import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#FFFFFF'
    },
    background: {
      default: '#F2F4F5',
      white: '#FFFFFF'
    },
  },
})

export default theme
